
Las carpetas ejemplifican los paquetes del programa 

Los id de los dispositivos, si se usa el archivo io.telemetry.data, hay variaciones si esta en un sistema operativo Linux deben ir sin comillas, si esta en windows entre comillas.
La razon es que al formar substrings del archivo, Linux solo pide reducir 1 elemento por lado, pero windows pide 2.

Al ser un archivo bastante grande demora el programa en compilar pero lo hace de manera correcta, esto depende del rango de fechas que provea al programa

Cada vez que se genera un archivo se muestra una pantalla de "Archivo creado" en netbeans, si no le salta en principal porfavor minimice todas las ventanas que posee y aparece
una vez da en aceptar el programa continua
